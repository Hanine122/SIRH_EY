@model IEnumerable<SIRH.EY.Models.Inscription>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateInscription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Terminee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Collaborateur)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateInscription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Terminee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Collaborateur.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Formation.Titre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
